import { CapitaPipePipe } from './capita-pipe.pipe';

describe('CapitaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
